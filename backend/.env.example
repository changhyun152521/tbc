# MongoDB Atlas 연결 문자열
# 형식: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/tbc-class

# 서버 포트 (선택)
PORT=3001

# JWT 시크릿 (배포 시 반드시 변경)
JWT_SECRET=your-secret-key-change-in-production

# CORS 허용 오리진 (배포 시 프론트엔드 URL, 쉼표 구분)
# 예: https://tbc-class.vercel.app
CORS_ORIGIN=
